" Solarized theme
syntax enable
set background=dark
colorscheme solarized

" GUI settings
if has("gui_running")
    " Simplify the ui
    set guioptions-=m
    set guioptions-=T
    " Set a prettier font.
    set guifont=SOURCE\ CODE\ PRO\ FOR\ POWERLINE
endif

